# Config

This directory contains application configuration and environment logic.

## Purpose
- Environment variable loading (`.env`)
- App-wide configuration classes (AppConfig)
- Feature flags

## Important
> [!IMPORTANT]
> This README file is a placeholder for documentation. **Please remove it** once real implementation files (e.g., `app_config.dart`) are added to this directory.
