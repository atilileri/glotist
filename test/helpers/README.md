# Test Helpers

This directory contains utilities, mock objects, and common setups used across multiple tests.

## Purpose
- `test_helpers.dart`: Shared test utility functions.
- `mock_dependencies.dart`: Centralized mock definitions (using mocktail/mockito).

## Important
> [!IMPORTANT]
> This README file is a placeholder for documentation. **Please remove it** once real test files are added to this directory.
